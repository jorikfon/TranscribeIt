<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Enable App Sandbox -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- User Selected Files (for opening audio files and saving transcriptions via dialog) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Network (for downloading Whisper models from Hugging Face) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- REMOVED: Microphone/Audio Input - NOT needed for file transcription -->
    <!-- We only process existing audio files, not live recording -->
</dict>
</plist>
